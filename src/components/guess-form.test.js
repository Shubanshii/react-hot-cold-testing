import React from 'react';
import {shallow} from 'enzyme';

import {GuessForm} from './board';

describe('<GuessForm />', () => {
    it('Renders without crashing', () => {
        shallow(<)
    });
});